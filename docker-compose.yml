version: '3.8'

services:
  antartida-labs:
    build: .
    container_name: antartida_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
